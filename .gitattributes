* text=auto eol=lf

# n8n workflow export: machine-serialized JSON. `linguist-detectable` opts JSON
# into language stats (linguist treats data formats as invisible by default),
# so the repository shows a language instead of none. `-diff` keeps pull
# requests readable, since the export is one very long line.
n8n-multi-agent-article-workflow.json linguist-language=JSON linguist-detectable -diff
